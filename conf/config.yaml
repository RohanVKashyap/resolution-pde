defaults:
  - model: fno_2d/fno_2d           
  - dataset: darcy_flow/darcy_flow
  - _self_  

# Training parameters
batch_size: 64
epochs: 500
learning_rate: 1e-3
use_normalizer: false
res_scale: 1

# Model selection
model_type: fno               # [fno, ffno, unet]

# Project and data parameters
checkpoint_dir: checkpoints